# Wallpaper Engine 壁纸引擎 — 注意事项与常见问题

## 目录

- [一、环境要求](#一环境要求)
- [二、壁纸文件相关](#二壁纸文件相关)
- [三、视频壁纸与 ffmpeg](#三视频壁纸与-ffmpeg)
- [四、渲染与兼容性](#四渲染与兼容性)
- [五、性能与资源](#五性能与资源)
- [六、配置注意事项](#六配置注意事项)
- [七、常见问题排查](#七常见问题排查)
- [八、开发者注意事项](#八开发者注意事项)

---

## 一、环境要求

### Java 版本
- **必须使用 Java 21 或更高版本**。构建工具和运行环境均要求 Java 21。
- 使用 Gradle 构建时会自动检查 Java 版本。

### Fabric 环境
- 需要 Fabric Loader ≥ 0.19.3 和 Fabric API。
- 使用 `splitEnvironmentSourceSets()` 分离客户端和服务端代码，服务端不会加载客户端相关代码。

---

## 二、壁纸文件相关

### 壁纸目录
- 壁纸目录为 `.minecraft/config/wallpaper/`（非 `config/wallpaper/` 的上级目录）。
- 首次启动游戏时模组自动创建该目录，如果未检测到壁纸文件会提示"未找到壁纸"。

### 静态图片
- 支持格式：PNG、JPG/JPEG、BMP、GIF
- 文件名中不支持的特殊字符会被自动替换为下划线 `_`，最终只保留 `[a-z0-9._-]`
- **GIF 仅提取第一帧作为静态图片**，不支持动图效果

### 文件名限制
- 壁纸文件名会转换为小写 ResourceLocation，格式为 `halei-mod:wallpaper/<文件名>`
- 文件名中的大写字母和特殊字符会被处理，可能出现重名覆盖的情况
- **建议使用英文、数字、横线、下划线命名**

---

## 三、视频壁纸与 ffmpeg

### ffmpeg 搜索机制
模组按以下优先级自动搜索 ffmpeg（只搜一次，搜不到则永久跳过视频壁纸）：

1. PATH 环境变量中的 `ffmpeg`
2. winget 安装目录的 ffmpeg
3. `C:\ffmpeg\bin\ffmpeg.exe`
4. `C:\Program Files\ffmpeg\bin\ffmpeg.exe`

### ⚠️ 重要限制
- **如果未安装 ffmpeg，所有视频文件都会被静默跳过**，日志仅输出 "未找到 ffmpeg，跳过视频壁纸"
- 视频播放依赖 ffmpeg 的 `rawvideo` 输出管道，需要 ffmpeg 支持 RGBA 格式
- 视频缩放使用 `scale` 滤镜，限制最大 1920×1080，宽高保持偶数
- 视频默认**循环播放**（`-stream_loop -1`），视频文件本身不会无限循环时会导致播放终止

### 播放速度
- 速度范围：0.5x ~ 5.0x（通过滑块或配置文件调整）
- 速度通过调整帧解码间隔实现，速度快时解码线程会丢弃积压帧以保持实时性
- 速度过快（≥ 4x）时视频可能跟不上实际时间，表现为丢帧

### 视频解码线程
- 每个视频壁纸在后台有一个独立解码线程（守护线程）
- 切换壁纸时旧视频的解码线程会自动停止
- 解码线程名称为 `halei-video-<壁纸名>`

---

## 四、渲染与兼容性

### ModernUI + Sodium 环境
- 同时安装 ModernUI 和 Sodium 时，常规的 `disableBlend` 方法可能失效，导致壁纸底层透出泥土背景
- **解决方案**：渲染壁纸前先绘制实心黑色背景（`0xFF000000`），代码已在 `renderMenuWallpaper` 中实现

### 主菜单渲染
- 使用 **三重保证** 确保壁纸正确显示：
  1. `@Inject` 在 TitleScreen.render() 的 HEAD 先绘制壁纸
  2. `@Redirect` 阻止全景背景（panorama）绘制覆盖壁纸
  3. `@Inject` 在 TitleScreen.renderBackground() 的 HEAD 取消默认渲染
- 当没有壁纸时，回退使用原版全景背景

### 所有菜单界面
- 通过 `@Mixin(Screen.class)` 的 `renderBackground` 统一注入
- **游戏中打开的界面**（level != null）不覆盖背景，保留半透明效果让游戏画面透出

### HUD AFK 叠加层
- 在 `Gui.render()` 的 TAIL 注入，位于所有 HUD 元素之上
- 透明度通过 `RenderSystem.setShaderColor` 的 alpha 通道控制
- 即使透明度为 0，视频帧仍在后台更新（保持壁纸切换时的即时性）

---

## 五、性能与资源

### GPU 纹理
- 静态壁纸使用 `DynamicTexture`（NativeImage），RGBA 格式
- 视频壁纸使用 **双 PBO（像素缓冲区对象）** 异步上传帧数据到 GPU
  - 双缓冲 ping-pong 机制：一个 PBO 正在上传，另一个 PBO 正在填充数据
  - 使用 `glMapBuffer(GL_WRITE_ONLY)` 映射写入，失败时回退到 `glTexSubImage2D` 直接上传

### 像素数据缓存
- 所有壁纸的 RGBA 像素数据会缓存在内存中（`wallpaperPixelData` 列表）
- 用于资源包重载后快速恢复纹理，占用内存 = 壁纸数 × 宽度 × 高度 × 4 字节
- **大量高分辨率壁纸可能导致内存占用较高**

### 视频帧更新
- 解码线程按帧间隔（由 fps 和速度共同决定）解码帧
- 主渲染循环（约 60fps）检查新帧，通过 PBO 异步上传
- 如果解码线程产出过快，积压帧会被丢弃（`pendingFrame` 非空时跳过）

---

## 六、配置注意事项

### 配置文件路径
`config/halei-mod.json`（相对于游戏运行目录）

### 配置项说明
| 配置项 | 说明 |
|--------|------|
| `playSpeed` | 视频播放速度，范围 0.5~5.0，超出范围会被自动裁剪 |
| `inGameKKey` | 游戏中按 K 打开壁纸选择界面的开关 |

### 注意事项
- 修改配置保存后**即时生效**，无需重启游戏
- 配置使用 Gson 序列化，具有可读格式（`setPrettyPrinting`）
- 配置不存在时会自动创建并写入默认值

---

## 七、常见问题排查

### 1. 壁纸不显示
- **检查壁纸目录**：`.minecraft/config/wallpaper/`，确保文件在其中
- **检查文件格式**：仅支持 PNG、JPG/JPEG、BMP、GIF（图片）；MP4、AVI、MKV、MOV、WebM（视频）
- **检查游戏日志**：搜索 `halei-mod/wallpaper` 的日志信息
- **检查控制提示**：按 K 时如果提示"未找到壁纸"，说明目录为空或不正确

### 2. 视频壁纸不播放
- **检查 ffmpeg 安装**：在命令行执行 `ffmpeg -version` 确认可用
- **检查游戏日志**：搜索 `未找到 ffmpeg` 提示
- **检查视频格式**：确保视频编码是 ffmpeg 支持的格式
- **检查视频尺寸**：过大视频可能超出解码能力

### 3. 壁纸颜色异常 / 花屏
- **检查 ModernUI + Sodium 兼容性**：如果安装了这两个模组，目前已有特殊处理
- **检查 OpenGL 版本**：需要支持 OpenGL 3.0+（PBO 特性需要）
- **PBO 映射失败时**：会自动回退到直接上传，可能略微影响帧率

### 4. 按键 K 无反应
- **检查控制绑定冲突**：其他模组可能占用了 K 键
- **检查配置**：`inGameKKey` 是否被设为 `false`
- **尝试主菜单**：如果游戏内不行，试试在主菜单按 K

### 5. 切换资源包后壁纸丢失
- 模组已注册资源重载监听器，切换资源包后会自动恢复壁纸纹理
- 如果仍丢失，尝试按 K 打开壁纸选择界面重新选择

---

## 八、开发者注意事项

### 构建命令
```bash
./gradlew build           # 构建模组
./gradlew clean build     # 清理并重新构建
```

构建产物位于 `build/libs/` 目录下。

### 代码结构
```
src/
├── client/                          # 客户端代码（仅客户端加载）
│   ├── java/com/example/halei/client/
│   │   ├── HaleiModClient.java      # 客户端初始化入口
│   │   ├── ModConfig.java           # 配置文件管理
│   │   ├── WallpaperManager.java    # 壁纸核心管理器（含视频播放器）
│   │   ├── WallpaperSelectionScreen.java  # 壁纸选择UI界面
│   │   └── mixin/                   # Mixin注入
│   │       ├── TitleScreenMixin.java       # 主菜单壁纸
│   │       ├── ScreenBackgroundMixin.java  # 菜单背景壁纸
│   │       ├── HudRenderMixin.java         # AFK叠加层
│   │       ├── InputMixin.java             # 鼠标AFK检测
│   │       ├── KeyboardInputMixin.java     # 键盘AFK检测
│   │       ├── ScreenAccessor.java         # Screen访问器
│   │       └── TitleScreenInvoker.java     # Invoker访问器
│   └── resources/halei-mod.client.mixins.json
└── main/
    ├── java/com/example/halei/
    │   ├── HaleiMod.java            # 模组主入口
    │   └── mixin/ExampleMixin.java   # 示例Mixin
    └── resources/
        ├── fabric.mod.json
        ├── halei-mod.mixins.json
        └── assets/halei-mod/lang/
            ├── zh_cn.json            # 中文翻译
            └── en_us.json            # 英文翻译
```

### 关键常量参考
| 常量 | 值 | 说明 |
|------|-----|------|
| AFK_TICKS | 6000（5分钟） | AFK 触发时间 |
| FADE_TO_HALF_MS | 5000ms | 透明度 0→50 |
| FADE_HALF_TO_FULL_MS | 5000ms | 透明度 50→100 |
| FADE_OUT_MS | 5000ms | 透明度 50→0 |
| loadDelayTicks | 100 ticks | 延迟加载（等 OpenGL 初始化） |
| 视频最大尺寸 | 1920×1080 | 缩放上限 |

### 已知限制
- **只支持静态 GIF（提取第一帧）**，不支持 GIF 动图
- 视频壁纸切换时，旧解码线程销毁和新线程启动之间可能有短暂黑屏
- PBO 在某些集成显卡上可能映射失败，会自动回退
- 资源重载后视频播放器需要重新捕获新纹理引用（已实现 `updateTexture`）
