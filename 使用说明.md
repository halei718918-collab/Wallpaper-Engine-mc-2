# Wallpaper Engine 壁纸引擎 — 使用说明

## 简介

本模组（halei-mod）为 Minecraft 1.21.1（Fabric）添加了动态壁纸系统。支持静态图片和视频壁纸，在主菜单显示精美背景，AFK 挂机时自动淡入壁纸叠加层。

---

## 一、安装要求

| 项目 | 要求 |
|------|------|
| Minecraft | 1.21.1 |
| Fabric Loader | ≥ 0.19.3 |
| Fabric API | 任意版本 |
| Java | ≥ 21 |
| ffmpeg（可选） | 如需播放视频壁纸 |

---

## 二、壁纸放置

1. 启动游戏一次，模组会自动创建 `config/wallpaper/` 目录
2. 将你的壁纸文件放入该目录

### 支持的格式

| 类型 | 格式 |
|------|------|
| 静态图片 | PNG、JPG/JPEG、BMP、GIF |
| 视频（需 ffmpeg） | MP4、AVI、MKV、MOV、WebM |

> **注意**：视频壁纸最大缩放至 1920×1080 以降低 GPU 消耗。

---

## 三、快捷键

| 按键 | 功能 |
|------|------|
| **K** | 打开壁纸选择界面（主菜单/游戏中均可使用） |

> 快捷键可在游戏选项 → 控制 → Wallpaper Engine 壁纸引擎 中自定义修改。

---

## 四、壁纸选择界面

按 **K** 键打开壁纸选择界面，你可以：

- 浏览所有壁纸（显示缩略图、文件名、尺寸、是否为视频）
- 点击壁纸项目 → 点击「选择」按钮切换壁纸
- 使用「播放速度」滑块调节视频播放速度（0.5x ~ 5.0x）
- 按 Esc 关闭界面

当前正在使用的壁纸会以 **绿色 ▶** 标记和 **✓ 当前** 标识。

---

## 五、AFK 自动壁纸

模组内置 AFK（Away From Keyboard）检测系统：

| 阶段 | 时间 | 透明度 | 说明 |
|------|------|--------|------|
| 玩家活动 | — | 0% | 壁纸完全透明，不影响游戏 |
| AFK 阶段 1 | 5 秒 | 0% → 50% | 刚进入 AFK，壁纸半透明淡入 |
| AFK 阶段 2 | 5 秒 | 50% → 100% | 完全淡入壁纸 |
| AFK 稳定 | 5 分钟后 | 100% | 壁纸完全显示 |
| 恢复活动 | 5 秒 | 50% → 0% | 鼠标/键盘活动后壁纸淡出 |

> AFK 触发时间：**5 分钟**无任何操作（鼠标移动、点击、滚轮、键盘按键）。

---

## 六、视频壁纸（ffmpeg 支持）

### 安装 ffmpeg

视频播放依赖 **ffmpeg**，模组会自动按以下顺序搜索：

1. **系统 PATH 环境变量**中的 `ffmpeg`（推荐）
2. **winget 安装目录**：`%LOCALAPPDATA%\Microsoft\WinGet\Packages\Gyan.FFmpeg_Microsoft.WinGet.Source_8wekyb3d8bbwe\`
3. **常见路径**：`C:\ffmpeg\bin\ffmpeg.exe`

> 如果未找到 ffmpeg，模组会跳过所有视频文件，仅显示静态图片。

### 播放原理

- 使用 ffmpeg 在后台解码视频帧（实时缩放至合适尺寸）
- 通过 PBO（像素缓冲区对象）异步上传帧数据到 GPU
- 支持循环播放（`-stream_loop -1`）
- 播放速度可通过滑块实时调整（0.5x ~ 5.0x）

### 性能建议

- 视频分辨率不宜过高，建议控制在 1920×1080 以内
- 多个视频壁纸之间切换时，旧视频播放器会自动停止

---

## 七、配置文件

配置文件位置：`config/halei-mod.json`

```json
{
  "playSpeed": 2.5,
  "inGameKKey": true
}
```

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `playSpeed` | float | 2.5 | 视频播放速度（0.5~5.0） |
| `inGameKKey` | boolean | true | 是否启用游戏中按 K 打开壁纸界面 |

> 编辑后无需重启游戏，大部分设置即时生效。

---

## 八、资源包切换

切换资源包后，壁纸纹理会自动重新加载，无需手动恢复。

---

## 九、主菜单壁纸按钮

在主菜单右下角（靠近辅助功能按钮）有一个 **「壁」** 字小按钮，点击可直接打开壁纸选择界面。
